import 'package:flutter/material.dart';

class ComprarItem extends StatelessWidget {
  const ComprarItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Tela comprar"),
      ),
    );
  }
}
